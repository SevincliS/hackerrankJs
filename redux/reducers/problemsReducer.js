const INITIAL_STATE = {
  current: {},
  problems: [],
};

const problemsReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state
  }
};


export default problemsReducer;